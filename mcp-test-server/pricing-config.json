{
  "_comment": "Sample PerToolCallPricingConfig (spec §3.1). Used identically against both test servers so the same billing test suite covers both protocol eras. get_time is deliberately absent — it exercises the 'valid tool, no price configured' unbilled path (spec §11).",
  "toolPrices": [
    { "toolName": "search", "pricePerCall": 0.05 },
    { "toolName": "generate_report", "pricePerCall": 0.50 },
    { "toolName": "run_heavy_task", "pricePerCall": 1.00 }
  ]
}
